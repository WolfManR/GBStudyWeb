<Window x:Class="ManagerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManagerWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Name="Main">
    <Window.DataContext>
        <local:MainVM/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <local:MetricsCard x:Name="CpuMetricsCard" DataContext="{Binding Cpu}"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Width="140">
            <Button Content="Add Metric" Command="{Binding AddMetricCommand}"/>
        </StackPanel>
    </Grid>
</Window>
